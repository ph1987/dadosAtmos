export class Urls {

    public urlApi: string = "";

    constructor() {
      
      this.urlApi = "http://localhost:?????";
      //const base = window.location.href.split("/").slice(0, 3).join("/");
      //this.urlApi =  `${base}/cooperativa.api`;
    }
}

//ng build --prod